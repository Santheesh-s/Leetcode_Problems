select product_name,sum(unit) as unit from Products p left join Orders o on o.product_id=p.product_id where order_date between "2020-02-01" AND "2020-02-29" group by p.product_id having sum(unit)>=100
